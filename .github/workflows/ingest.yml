name: Kroger Daily Ingest

on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase Edge Function
        run: |
          curl -sS -X POST "https://hkrlzrsndekjmcrbazux.functions.supabase.co/kroger-ingest" \
            -H "x-ingest-secret: ${{ secrets.INGEST_SECRET }}"
